<odoo>

    <!--claim info-->
    <record model="ir.ui.view" id="tree_insurance_claim">
        <field name="name">tree.insurance.claim</field>
        <field name="model">insurance.claim</field>
        <field name="priority" eval="16"/>
        <field name="arch" type="xml">
            <tree string="list">
                <field name="policy_number"/>
                <field name="renewal"/>
                <field name="endorsement"/>
                <field name="details"/>
                <field name="amount"/>
            </tree>
        </field>
    </record>
    <record model="ir.ui.view" id="form_insurance_claim">
        <field name="name">form.insurance.claim</field>
        <field name="model">insurance.claim</field>
        <field name="priority" eval="16"/>
        <field name="arch" type="xml">
            <form string="Idea form">
                <group colspan="4">
                    <separator string="Create New Claim" colspan="2"/>
                    <field name="policy_number"/>
                    <field name="renewal"/>
                    <field name="endorsement"/>
                    <field name="risk_person"/>
                    <field name="risk_vehicle"/>
                    <field name="risk_cargo"/>
                    <field name="coverage"/>
                    <field name="details"/>
                    <field name="amount"/>
                </group>
            </form>
        </field>
    </record>
    <record model="ir.actions.act_window" id="insurance_claim_action">
        <field name="name">Claim</field>
        <field name="res_model">insurance.claim</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">Create New Claim
            </p>
        </field>
    </record>
</odoo>